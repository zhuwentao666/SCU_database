最后一个规模测试怎么样都跑不通。很奇怪，这个测试在根据某一个key来找一个叶子节点的时候会遇到一个空的内部节点。也就是Getsize()==0。我尝试打印findleafpage中遍历的所有节点的size，前100多个都很正常，至少都是它们的大小20但是
是突然就遍历到了一个空的内部节点。我依次检查了其他所有可能出现空节点的操作，确认了我将可能出现空节点时，我将它从树中甚至内存中删除了，却还是会遇到空的内部节点。
